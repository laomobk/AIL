
class Person {
    func __init__(self, name) {
        self.name = name;
    }

    func say(self, prompt='Hello', ret=False) {
        if ret { return '%s %s' % (prompt, self.name); }
        print prompt, self.name;
    }
}


print Person('Klee').say(*('你好',), 1)
