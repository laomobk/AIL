begin


fun f() is
    name = 'Nezha'
    
    fun inner() is
        print outer
        print outer.name
    end

    name = 'Zhaer'

    return inner
end


fun hello() is
    print 'Nezha'
end


fun say_hi(func) is
    fun wrapper() is
        print 'Hello'
        return outer.func()
    end
    return wrapper
end

hello = say_hi(hello)

inf = f()

inf()
print inf

hello()

end
